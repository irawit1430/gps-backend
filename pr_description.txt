🎯 **What:** Added tests for POST `/api/schools/:schoolId/students` and `/api/students`.

📊 **Coverage:** Tests now cover:
- Student creation without a parent.
- Student creation linking to an existing parent (skipping auto-invite).
- Student creation where a new parent must be invited (auto-invite and temporary password generation).
- Missing schoolId returning 400.
- Missing student name returning 400.
- Duplicate RFID tag handling returning 400.
- Database errors returning 500.

✨ **Result:** Improved test reliability and 100% test coverage achieved for this endpoint's complex branching.
