🎯 **What:** The testing gap addressed
Added a missing test for the `GET /api/schools/:schoolId/buses` endpoint to verify that it successfully fetches data using Prisma and correctly maps `driverName` and `routeName` in the response.

📊 **Coverage:** What scenarios are now tested
- Happy path: Verifies that the endpoint successfully returns a list of formatted buses with active trip info, ensuring the `driverName` and `routeName` are correctly mapped from the `trips` relation.
- Error path: Verifies that the endpoint correctly handles and returns a 500 error when the database throws an error.

✨ **Result:** The improvement in test coverage
The `GET /api/schools/:schoolId/buses` endpoint is now fully tested, ensuring its reliability and preventing future regressions when mapping nested Prisma relations.
